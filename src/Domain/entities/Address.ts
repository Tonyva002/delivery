export interface Address {
        id?: string,
        address: string,   
        neighborhood: string,
        zipcode: string,
        city: string,
        country: string,
        lat: number,
        lng: number,
        id_user: string

}